const basePath = window.location.hostname.includes("localhost") ? "/" : "/react-tools/";

export const ROUTES = {
    HOMEPAGE_ROUTE: basePath,
    SVG_REACT_CONVERTER: basePath+'svg-react-converter'
}
